import { FileSystemEntriesOptions } from './file-system-entries-options';

describe('FileSystemEntriesOptions', () => {
  it('should create an instance', () => {
    expect(new FileSystemEntriesOptions()).toBeTruthy();
  });
});
